import styled from 'styled-components';
import {
	bodyContainer,
	flexAlignCenter,
	flexColumn,
} from '../../../styles/common';

const S = {
	Container: styled.div`
		padding: 4.7rem 0 7.35rem;
		${bodyContainer}
	`,
};

export default S;
