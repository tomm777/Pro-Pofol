import styled from 'styled-components';
import {
	bodyContainer,
	flexAlignCenter,
	flexColumn,
} from '../../../../styles/common';

export const PostCardContainer = styled.div`
	${flexColumn}
	gap: 32px;
`;
